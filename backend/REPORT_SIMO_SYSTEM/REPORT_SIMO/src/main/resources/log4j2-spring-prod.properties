# # === Base Properties ===
# property.LOG_DIR = ${env:LOG_DIR:-/simo_log}
# property.LOG_PATTERN = %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %c{1} - %msg%n

# # === Console Appender ===
# appender.console.type = Console
# appender.console.name = CONSOLE
# appender.console.layout.type = PatternLayout
# appender.console.layout.pattern = ${LOG_PATTERN}

# # === Daily Rolling File Appender ===
# appender.file.type = RollingFile
# appender.file.name = DAILY_LOG
# appender.file.fileName = ${LOG_DIR}/current.log
# appender.file.filePattern = ${LOG_DIR}/%d{yyyyMMdd}.log
# appender.file.layout.type = PatternLayout
# appender.file.layout.pattern = ${LOG_PATTERN}
# appender.file.policies.type = Policies
# appender.file.policies.time.type = TimeBasedTriggeringPolicy
# appender.file.policies.time.interval = 1
# appender.file.policies.time.modulate = true

# # === Root Logger ===
# rootLogger.level = info
# rootLogger.appenderRef.console.ref = CONSOLE
# rootLogger.appenderRef.daily.ref = DAILY_LOG
